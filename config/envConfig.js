const envConfig = {
	serverURL: process.env.SERVER_URL,
	tokenUser: process.env.TOKEN_USER,
	tokenPassword: process.env.TOKEN_PASSWORD,
	socialPassword: process.env.USER_SOCIAL_PASSWORD,
};

export default envConfig;
